syntax = "proto3";

package user.entity;

message User {
  string id = 1; // user_id (UUID)
  string name = 2; // user_name (and display_name)
  bytes password = 3; // user_password (hashed)
  uint64 score = 4; // game_score
}
